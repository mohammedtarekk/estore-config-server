server:
  port: 8081

spring:
  application:
    name: inventory
  profiles:
    active: default
  datasource:
    url: jdbc:postgresql://localhost:5432/payment
    username: postgres
    password: '{cipher}293996a59cb92ec86a5191de2202dbd19865b45d382e9e9ff957de5f0059f723'

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

# configure info endpoint (actuator)
info:
  app:
    name: Inventory
    description: It will hold products
    version: 1.0.0
